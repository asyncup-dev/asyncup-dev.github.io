import{_ as e,o,c as s,a2 as r}from"./chunks/framework.CNDaxUod.js";const g=JSON.parse('{"title":"Installing AsyncUp for your team","description":"","frontmatter":{},"headers":[],"relativePath":"guide/distribution.md","filePath":"guide/distribution.md"}'),a={name:"guide/distribution.md"};function n(i,t,l,d,h,p){return o(),s("div",null,[...t[0]||(t[0]=[r('<h1 id="installing-asyncup-for-your-team" tabindex="-1">Installing AsyncUp for your team <a class="header-anchor" href="#installing-asyncup-for-your-team" aria-label="Permalink to &quot;Installing AsyncUp for your team&quot;">​</a></h1><p>AsyncUp is <strong>fully self-hosted</strong>, which means <em>you</em> (the operator) own all three moving parts, and they&#39;re inseparable:</p><ol><li><strong>Server + database</strong> — your container (this is what you run)</li><li><strong>GCP project</strong> — the Chat API config + service account</li><li><strong>The Chat app on Google&#39;s side</strong> — what users actually install</li></ol><p>There is <strong>no central AsyncUp app on the Google Workspace Marketplace</strong> that everyone shares — there can&#39;t be. A Marketplace listing is <a href="https://developers.google.com/workspace/marketplace/enable-configure-sdk" target="_blank" rel="noreferrer">tied to one Google Cloud project</a>, and a Chat app points at exactly <a href="https://developers.google.com/workspace/add-ons/chat/configure" target="_blank" rel="noreferrer">one HTTP endpoint</a>. A single published app routes every install to that one endpoint — so the only way one listing could serve many orgs is if they all talked to the same server (that&#39;s SaaS). Because AsyncUp runs on <em>your</em> server, <strong>you register your own Chat app in your own Google project.</strong> Your data and credentials never touch the maintainer or any other org.</p><p>This page is only about step 3 — <strong>how your team gets the bot.</strong> For the GCP project + service-account setup, do <a href="./google-chat-setup.html">Google Chat setup</a> first.</p><h2 id="the-principle-that-should-decide-your-path" tabindex="-1">The principle that should decide your path <a class="header-anchor" href="#the-principle-that-should-decide-your-path" aria-label="Permalink to &quot;The principle that should decide your path&quot;">​</a></h2><p>A standup bot only works if <strong>everyone reliably receives their DM prompt</strong> — the bot can only message someone it shares a DM space with. So the real question is whether you can count on each person to add the bot themselves, or whether you want it installed <em>for</em> them.</p><table tabindex="0"><thead><tr><th></th><th>Path A — Allowlist + self-add</th><th>Path B — Private Marketplace + admin install</th></tr></thead><tbody><tr><td>Best for</td><td>small teams (≤ a handful, or one Google Group)</td><td>whole org / departments, mandatory standups</td></tr><tr><td>Setup effort</td><td>minutes, no Marketplace</td><td>~1 hour one-time (listing + assets)</td></tr><tr><td>Who installs</td><td>each user adds the bot once</td><td>admin force-installs for an OU/group</td></tr><tr><td>Risk</td><td>someone forgets → silently never prompted</td><td>none — everyone gets it</td></tr></tbody></table><h2 id="path-a-—-visibility-allowlist-self-add-small-teams" tabindex="-1">Path A — Visibility allowlist + self-add (small teams) <a class="header-anchor" href="#path-a-—-visibility-allowlist-self-add-small-teams" aria-label="Permalink to &quot;Path A — Visibility allowlist + self-add (small teams)&quot;">​</a></h2><p>No Marketplace listing needed.</p><ol><li><strong>Admin console</strong> → Apps → Google Workspace → Google Chat → <strong>Chat apps</strong> → set <strong>&quot;Allow users to install Chat apps&quot;</strong> to <strong>On</strong> (<a href="https://support.google.com/a/answer/7651360" target="_blank" rel="noreferrer">docs</a>).</li><li>In your Chat app&#39;s <strong>Configuration</strong> (the GCP project), set <strong>Visibility</strong> to the people who&#39;ll use it. Google allows <strong>up to five individuals, or one or more Google Groups</strong> from your domain (<a href="https://developers.google.com/workspace/add-ons/chat/configure" target="_blank" rel="noreferrer">docs</a>) — so for more than five people, point it at a <strong>Google Group</strong>.</li><li>Each user opens Google Chat → <strong>+ → Find apps → AsyncUp → Add</strong>, or just messages the bot once. That first interaction creates the DM space so AsyncUp can prompt them.</li></ol><p>That&#39;s it. The catch is step 3: anyone who never adds the bot won&#39;t be prompted, and you won&#39;t get an error — they&#39;re just quietly absent. Fine for a team you can nudge in a channel; not great for a mandatory company-wide standup.</p><h2 id="path-b-—-private-marketplace-listing-admin-install-org-wide-zero-touch" tabindex="-1">Path B — Private Marketplace listing + admin install (org-wide, zero-touch) <a class="header-anchor" href="#path-b-—-private-marketplace-listing-admin-install-org-wide-zero-touch" aria-label="Permalink to &quot;Path B — Private Marketplace listing + admin install (org-wide, zero-touch)&quot;">​</a></h2><p>This makes AsyncUp installable org-wide and lets an admin <strong>force-install</strong> it, so every targeted user (and every future new hire in the OU) gets the bot with a DM space automatically — no action from them.</p><p><strong>1. Publish a private listing</strong> via the <strong>Google Workspace Marketplace SDK</strong> in the <em>same</em> GCP project as your Chat app (<a href="https://developers.google.com/workspace/marketplace/how-to-publish" target="_blank" rel="noreferrer">docs</a>):</p><ul><li>Enable the Marketplace SDK, then <strong>APIs &amp; Services → Google Workspace Marketplace SDK → App Configuration</strong>: App Visibility = <strong>Private</strong> (your domain only), Installation = <strong>Admin Install</strong> (and/or Individual), App Integrations = your Chat app.</li><li><strong>OAuth consent screen = Internal</strong> — a private app to your own domain skips Google&#39;s public verification review.</li><li><strong>Store Listing</strong> (all required to publish): name, short/long description, <strong>category</strong>, <strong>pricing = Free</strong>, icons (<strong>32×32</strong>, <strong>128×128</strong>), a <strong>220×140</strong> banner, <strong>≥1 screenshot</strong>, and <strong>Terms of Service / Privacy Policy / Support</strong> URLs. <em>(AsyncUp ships ready-made icons, a banner, and a screenshot — see the repo&#39;s <code>docs/public</code> assets — so this is mostly paste-and-upload.)</em></li><li><strong>Publish.</strong></li></ul><p><strong>2. Install it</strong> (<a href="https://support.google.com/a/answer/6089179" target="_blank" rel="noreferrer">docs</a>): <strong>Admin console → Apps → Google Workspace Marketplace apps → Apps list → Add app to allowlist / Install</strong>, scoped to the OU or group that should have AsyncUp.</p><blockquote><p><strong>Publish ≠ install.</strong> Publishing the listing only makes it <em>available</em>. Nobody has the bot until you complete the install/allowlist step in the Admin console. This is the single most common place people get stuck.</p></blockquote><h2 id="set-the-request-audience-to-project-number" tabindex="-1">Set the request audience to &quot;Project Number&quot; <a class="header-anchor" href="#set-the-request-audience-to-project-number" aria-label="Permalink to &quot;Set the request audience to &quot;Project Number&quot;&quot;">​</a></h2><p>In your Chat app&#39;s <strong>Connection settings</strong>, the <strong>Authentication Audience</strong> field has two modes (<a href="https://developers.google.com/workspace/chat/verify-requests-from-chat" target="_blank" rel="noreferrer">docs</a>):</p><ul><li><strong>Project Number</strong> <em>(recommended)</em> — Google sends a JWT signed by <code>chat@system.gserviceaccount.com</code> whose <code>aud</code> is your project <strong>number</strong>. This is exactly what AsyncUp verifies. Paste that number into the dashboard&#39;s <strong>Audience</strong> field (Settings → Google Chat).</li><li><strong>HTTP endpoint URL</strong> — Google instead sends an OIDC ID token whose <code>aud</code> is your endpoint URL. AsyncUp&#39;s dashboard will accept a URL here, but the recommended, fully-supported mode is <strong>Project Number</strong> — use it unless you have a specific reason not to.</li></ul><h2 id="who-s-responsible-for-what-google-side" tabindex="-1">Who&#39;s responsible for what (Google side) <a class="header-anchor" href="#who-s-responsible-for-what-google-side" aria-label="Permalink to &quot;Who&#39;s responsible for what (Google side)&quot;">​</a></h2><table tabindex="0"><thead><tr><th></th><th>Maintainer (the project)</th><th>You (operator)</th><th>Google</th></tr></thead><tbody><tr><td>GCP project + Chat app + service-account key</td><td>ships docs + avatar asset</td><td><strong>owns &amp; configures</strong></td><td>provides the APIs</td></tr><tr><td>Distribution choice (allowlist vs listing)</td><td>documents both</td><td><strong>chooses &amp; configures</strong></td><td>hosts Marketplace</td></tr><tr><td>Admin install / domain-wide delegation</td><td>documents</td><td><strong>performs in your Admin console</strong></td><td>enforces it</td></tr><tr><td>Marketplace listing + its legal URLs</td><td>provides a starter kit</td><td><strong>owns it</strong> (it&#39;s in your project)</td><td>reviews public listings only</td></tr><tr><td>Standup data + credentials</td><td><strong>no access, ever</strong></td><td><strong>data controller</strong></td><td>stores nothing beyond delivery</td></tr></tbody></table><p>The maintainer has <strong>zero access</strong> to your Google project, keys, or data — that&#39;s the point of self-hosting.</p><h2 id="what-it-costs" tabindex="-1">What it costs <a class="header-anchor" href="#what-it-costs" aria-label="Permalink to &quot;What it costs&quot;">​</a></h2><p><strong>$0 on the Google side, every path.</strong> Registering a Chat app, publishing a <strong>private</strong> Marketplace listing, and admin-installing it are all free; Chat API and Calendar API usage is free within quota; an internal app skips paid verification. You already pay for Google Workspace (Chat is included). Your only real cost is the server that runs AsyncUp — see <a href="./server-setup.html#system-requirements">Setup guide</a> (≈ $0 if you co-locate on existing infra, ≈ $5/mo otherwise).</p><h2 id="asyncup-not-responding-—-checklist" tabindex="-1">&quot;AsyncUp not responding&quot; — checklist <a class="header-anchor" href="#asyncup-not-responding-—-checklist" aria-label="Permalink to &quot;&quot;AsyncUp not responding&quot; — checklist&quot;">​</a></h2><p>If <code>@AsyncUp setup</code> says the app isn&#39;t responding, Google usually isn&#39;t reaching your endpoint. Check, in order:</p><ol><li><strong>App status = Live</strong> in the Chat API Configuration.</li><li><strong>Visibility is not empty</strong> — a blank allowlist (&quot;Input is required&quot;) means the app is visible to <em>nobody</em>. Add the people/Group (Path A) or publish + install (Path B).</li><li><strong>Installed, not just published</strong> — complete the Admin-console install (Path B).</li><li><strong>Endpoint reachable over HTTPS</strong> — <code>curl -i -X POST -H &#39;content-type: application/json&#39; -d &#39;{}&#39; https://&lt;your-host&gt;/chat/events</code> should answer with JSON (a <code>401</code> is fine; it means the app is up and rejecting an unsigned probe — a plain GET returns 404, the route is POST-only). The app logs every arrival and the concrete 401 reason, so check <code>docker compose logs</code> to see whether Google&#39;s request even arrived.</li><li><strong>Audience matches</strong> — dashboard Audience = your project <strong>number</strong>, with Authentication Audience = <strong>Project Number</strong> on the Google side.</li></ol>',29)])])}const u=e(a,[["render",n]]);export{g as __pageData,u as default};
